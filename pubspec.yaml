name: marketplace_3d
description: Marketplace movil con generacion 3D y vista AR
publish_to: none
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

  image_picker: ^1.1.2
  path_provider: ^2.1.4
  path: ^1.9.0

  http: ^1.2.2
  dio: ^5.7.0

  provider: ^6.1.2

  firebase_core: ^3.6.0
  firebase_auth: ^5.3.1
  cloud_firestore: ^5.4.3
  google_sign_in: ^6.2.1
  firebase_messaging: ^15.1.2

  intl: ^0.19.0
  flutter_dotenv: ^5.2.1

  permission_handler: ^11.3.1

  model_viewer_plus: ^1.9.3
  url_launcher: ^6.3.0
  device_info_plus: ^10.1.2
  camera: ^0.11.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  mocktail: ^1.0.4

flutter:
  uses-material-design: true
  assets:
    - .env
